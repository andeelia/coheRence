overview of all required functionalities:

1: List all .tif and clip them with osm building data
    -> get building and federal border data automatically, based on the extent of the .tif files !HELL NO
    -> if crs are different, transform all Items in the list to the predefined one
    -> integrate dynamic extent definition via interactive map !NO

2: look at one image and count coherence pixels per inside each overlaying building polygon
    -> calculate the mean or median coherence value of each building
    -> store the values of all images in the list in one .DF and in a .gpkg

3: plot the resulting DF in percent graphs and in line charts for time series analysis
    -> save them as .png


#%%TEST AREA

#define personal variables
path <- '/home/andeelia/Documents/GitHub/package_test/raw_data/'
my_crs <- "EPSG:32636" 
final_dir <- '/home/andeelia/Documents/GitHub/package_test/clipped_data/'
buildings <- '/home/andeelia/Documents/GitHub/package_test/raw_data/Gaza_Stripe_buildings.shp'



#call function
test <- load_and_clip(data_path=path, target_crs=my_crs, buildings_path = buildings, save_clips = TRUE, project_path = final_dir)

clipped_raster <- test[[1]]
clipped_buildings <- test[[2]]
image_count <- test[[3]]

test2 <- coh_calc(rast_files = clipped_raster, buildings = clipped_buildings, target_crs =  my_crs, project_path = final_dir)

classified_plots(coh_df = test2, number_of_images = image_count, project_path = final_dir)